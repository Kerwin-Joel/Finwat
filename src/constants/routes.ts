export const ROUTES = {
    HOME: '/',
    LOGIN: '/login',
    REGISTER: '/register',
    TRANSACTIONS: '/transactions',
    ACCOUNTS: '/accounts',
    SERVICES: '/services',
    PROFILE: '/profile',
} as const;
